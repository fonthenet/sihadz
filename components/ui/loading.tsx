import { FullPageLoading } from '@/components/ui/page-loading'

/** Default loading component - use for Suspense fallbacks and route loading.tsx */
export default function Loading() {
  return <FullPageLoading />
}
