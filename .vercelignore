# Exclude from deployment to reduce package size (build still uses app/, components/, lib/)
# Root settings/ is a standalone workspace duplicate - not used by main app
settings/
# Android native - not needed for web deployment
android/
# IDE and tooling
.cursor/
.vscode/
# Tests - not needed for production build
__tests__/
# Documentation
docs/
# Development scripts (keep scripts/write-build-info.js via negation - .vercelignore doesn't support that, so we can't exclude scripts)
